import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        marginTop:24,
        display:'flex',
        flex: 1,
        alignItems: 'center', 
        justifyContent: 'space-around',
    },  
});